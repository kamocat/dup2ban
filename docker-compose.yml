services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Mount config.json so you can edit settings without rebuilding
      - ./config.json:/app/config.json:ro
